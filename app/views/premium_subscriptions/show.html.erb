<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true" id="premiumModal">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="container py-3">
        <div class="row">
          <div class="col-sm-12 col-md-6">

          </div>
          <div class="col-sm-12 col-md-6">
            <h2><%= @premium_subscription.name %></h2>
            <p class="card-text">.</p>

            <p>$$$</p>

            <%= form_tag orders_path do %>
            <%= hidden_field_tag 'premium_subscription_id', @premium_subscription.id %>
            <%= submit_tag 'Purchase', class: 'btn btn-primary' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
