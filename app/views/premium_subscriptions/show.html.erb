<div class="container py-3">
  <div class="row">
    <div class="col-sm-12 col-md-6">

    </div>
    <div class="col-sm-12 col-md-6">
      <h1><%= @premium_subscription.name %></h1>
      <p>Some awesome description of our amazing premium_subscription.</p>
      <p>$$$</p>

      <%= form_tag orders_path do %>
      <%= hidden_field_tag 'premium_subscription_id', @premium_subscription.id %>
      <%= submit_tag 'Purchase', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
