<div class="background">
  <h2 class="page-h2">Vos messages</h2>
    <% @chatrooms.each do |chatroom| %>
     <%= link_to chatroom.name, chatroom_path(chatroom, anchor: "envoie") %></li>
    <% end %>
</div>
