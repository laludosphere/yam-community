<div class="background" style="height: 100%;">
  <div class="fixed-footer">
    <div class="container">
      <div class="row py-4">
        <div class="col">
          <h1>Commande #<%= @order.id %> <small><span class="badge badge-primary"><%= @order.state %></span></small></h1>
          <ul class="list-unstyled">
            <li><strong>Souscription Premium</strong> <%= @order.premium_subscription.name %> [<%= @order.premium_subscription_sku %>]</li>
            <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
